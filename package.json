{
  "name": "flyersnap",
  "private": true,
  "description": "Tooling only. The app itself has no dependencies and no build step - index.html is self-contained by design (CLAUDE.md rule 4). Nothing here is loaded by the app at runtime.",
  "scripts": {
    "test": "node tests.js",
    "audit": "node tools/a11y-audit.js",
    "preview": "node tools/preview.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
